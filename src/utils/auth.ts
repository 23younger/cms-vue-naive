// import { router } from "@/router";

// 处理跳转到登录页的逻辑，有可能在客户端里直接调用客户端提供的logout方法并跳转到
// 客户端登录页上
export function toLogin() {
  console.log("tianzhuan denglu");
  // 应该跳转到客户端的登录 callbackObj.tologin
  // router.replace({ path: "/login" });
}
